{
  "name": "custom_plugin",
  "version": "1.0.0",
  "description": "A custom plugin for OpenSearch Dashboards",
  "main": "target/server",
  "scripts": {
    "build": "yarn plugin-helpers build",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@opensearch-dashboards/core": "^2.14.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^14.18.63",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^4.9.5"
  }
}
